# Development dependencies. Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=7.0
pytest-cov>=4.0
ruff>=0.4
PyYAML>=6.0

# Optional backends. Neither is required to run the core test suite.
# tensorflow>=2.12
# pybind11>=2.11
